{
  "name": "@growae/reactive-wasm-js-binding",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "WIT world and jco-transpiled JS glue for the differential harness. Not published.",
  "scripts": {
    "build:rust": "cd core-component && cargo component build --release --target wasm32-unknown-unknown",
    "build:js": "rm -rf generated && mkdir generated && jco transpile core-component/target/wasm32-unknown-unknown/release/core_component.wasm -o generated --name core-harness --minify",
    "build": "pnpm run build:rust && pnpm run build:js",
    "test": "vitest run"
  },
  "devDependencies": {
    "@bytecodealliance/jco": "1.29.0",
    "vitest": "^3.2.7"
  }
}
