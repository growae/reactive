[package]
name = "ae-parity"
description = "The differential parity harness: the coverage matrix, and the signer for the on-node exercise"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true

# A measuring instrument, not a shipped library. It links both crates under test
# and reads their committed reference corpora; nothing here is compiled into a
# binding, and nothing in `ae-core` or `ae-fate` may ever depend on it.
[dependencies]
ae-core.workspace = true
ae-fate.workspace = true
serde_json.workspace = true

[lints]
workspace = true
